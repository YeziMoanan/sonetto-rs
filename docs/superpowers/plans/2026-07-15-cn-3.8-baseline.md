# 国服 3.8 独立基线实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 从当前可运行的国际服 3.6 工作树创建可构建、可测试、数据与端口隔离的 `sonetto-rs-3.8-cn` 基线。

**Architecture:** 使用本地 Git 克隆保留历史，再把源工作树中尚未提交但已参与运行的源码和开发文件覆盖到目标仓库，并在目标仓库固化为单独的基线提交。3.8 通过受测试的嵌入式配置模板使用独立端口和数据库，由 PowerShell 准备脚本生成不纳入 Git 的运行目录；国际服 3.6 源目录保持原样。

**Tech Stack:** Rust 2024、Cargo workspace、TOML、PowerShell 5.1+、Git、Robocopy、SQLite、Windows loopback。

---

## 计划范围

本计划只实施设计规格中的阶段 0。SDK 登录、TCP 协议识别和大厅初始化分别在基线通过后编写后续计划，因为它们需要真实 3.8 请求和命令证据。

执行期间只使用非交互文件与终端操作。不得执行系统或界面控制，不得修改任何 Windows、WinHTTP、环境变量或 Android 代理设置。

## 文件结构

执行后目标仓库包含以下新增或修改文件：

```text
D:\python-tools\重返未来1999\sonetto-rs-3.8-cn\
  .gitignore                              Ignore isolated runtime artifacts
  common\Config.toml                     Embedded CN 3.8 config template
  common\src\config.rs                  Config-template contract test
  docs\CN_3.8_DEVELOPMENT.md             Baseline build and runtime workflow
  docs\baseline\source-3.6-manifest.json Source working-tree seed manifest
  docs\baseline\target-3.8-manifest.json Copied seed manifest
  docs\baseline\source-status.txt        Source Git status at copy time
  scripts\Prepare-Cn38Runtime.ps1         Prepare isolated runtime layout
  scripts\tests\Test-PrepareCn38Runtime.ps1
                                           Black-box runtime-preparation test
  runtime\                                Generated and Git-ignored
    config.toml
    sdkserver.exe
    gameserver.exe
    data\excel2json\
    data\static\
    db\
```

`runtime` 不包含复制自 3.6 的数据库。初始 `data` 是独立复制的 3.6 引导数据，只用于服务启动和后续 3.8 协议分析。

### Task 1: 创建并固化国际服 3.6 工作基线

**Files:**
- Create repository: `D:\python-tools\重返未来1999\sonetto-rs-3.8-cn`
- Create: `docs/baseline/source-3.6-manifest.json`
- Create: `docs/baseline/target-3.8-manifest.json`
- Create: `docs/baseline/source-status.txt`

- [ ] **Step 1: 校验源目录、目标目录和源 Git 状态**

Run from `D:\python-tools\重返未来1999`:

```powershell
$ErrorActionPreference = "Stop"
$source = "D:\python-tools\重返未来1999\sonetto-rs"
$target = "D:\python-tools\重返未来1999\sonetto-rs-3.8-cn"

if (-not (Test-Path -LiteralPath "$source\.git" -PathType Container)) {
    throw "Source Git repository not found: $source"
}
if (Test-Path -LiteralPath $target) {
    throw "Target already exists: $target"
}

$sourceHead = (git -C $source rev-parse HEAD).Trim()
if ($LASTEXITCODE -ne 0) { throw "Unable to read source HEAD" }
$sourceStatus = @(git -C $source status --porcelain=v1 -uall)

[PSCustomObject]@{
    source = $source
    head = $sourceHead
    changed_entries = $sourceStatus.Count
} | ConvertTo-Json -Compress
```

Expected: source HEAD is present, target is absent, and the reported changed-entry count records the current dirty 3.6 baseline without modifying it.

- [ ] **Step 2: 创建独立本地克隆和功能分支**

```powershell
$source = "D:\python-tools\重返未来1999\sonetto-rs"
$target = "D:\python-tools\重返未来1999\sonetto-rs-3.8-cn"

git clone --no-hardlinks $source $target
if ($LASTEXITCODE -ne 0) { throw "Local clone failed" }

git -C $target switch -c feat/cn-3.8-login-lobby
if ($LASTEXITCODE -ne 0) { throw "Feature branch creation failed" }

git -C $target remote rename origin source-3.6
if ($LASTEXITCODE -ne 0) { throw "Remote rename failed" }

git -C $target remote set-url --push source-3.6 no_push
if ($LASTEXITCODE -ne 0) { throw "Push disable failed" }
```

Expected: target branch is `feat/cn-3.8-login-lobby`; fetch remote is the local 3.6 repository and push URL is the non-routable literal `no_push`.

- [ ] **Step 3: 覆盖当前有效工作树并排除运行产物**

```powershell
$source = "D:\python-tools\重返未来1999\sonetto-rs"
$target = "D:\python-tools\重返未来1999\sonetto-rs-3.8-cn"

$robocopyOutput = & robocopy.exe $source $target /E /COPY:DAT /DCOPY:DAT /R:1 /W:1 `
    /XD "$source\.git" "$source\target" "$source\runtime" `
    /XF *.log *.err.log *.bak *.db
$robocopyExit = $LASTEXITCODE
if ($robocopyExit -gt 7) {
    throw "Robocopy failed with exit code $robocopyExit`: $($robocopyOutput -join [Environment]::NewLine)"
}

$forbidden = @(
    Get-ChildItem -LiteralPath $target -Recurse -File -Force |
        Where-Object {
            $_.FullName -notlike "$target\.git\*" -and
            ($_.Name -like "*.log" -or $_.Name -like "*.bak" -or $_.Name -like "*.db")
        }
)
if ($forbidden.Count -ne 0) {
    throw "Forbidden runtime artifacts copied: $($forbidden.FullName -join ', ')"
}
```

Expected: Robocopy exit code is between `0` and `7`; no log, backup, or database exists outside the target `.git` directory.

- [ ] **Step 4: 生成并比较源、目标种子清单**

```powershell
$source = "D:\python-tools\重返未来1999\sonetto-rs"
$target = "D:\python-tools\重返未来1999\sonetto-rs-3.8-cn"
$sourceHead = (git -C $source rev-parse HEAD).Trim()
$sourceStatus = @(git -C $source status --porcelain=v1 -uall)

function Get-SeedManifestFiles {
    param([Parameter(Mandatory = $true)][string]$Root)

    $rootPath = (Resolve-Path -LiteralPath $Root).Path
    @(
        Get-ChildItem -LiteralPath $rootPath -Recurse -File -Force |
            ForEach-Object {
                $relative = $_.FullName.Substring($rootPath.Length).TrimStart('\').Replace('\', '/')
                $segments = $relative.Split('/')
                if ($segments -contains '.git' -or $segments -contains 'target' -or $segments -contains 'runtime') {
                    return
                }
                if ($_.Name -like '*.log' -or $_.Name -like '*.bak' -or $_.Name -like '*.db') {
                    return
                }
                [PSCustomObject][ordered]@{
                    path = $relative
                    length = [int64]$_.Length
                    sha256 = (Get-FileHash -LiteralPath $_.FullName -Algorithm SHA256).Hash
                }
            } |
            Sort-Object path
    )
}

$sourceFiles = @(Get-SeedManifestFiles -Root $source)
$targetFiles = @(Get-SeedManifestFiles -Root $target)
$sourceComparable = @($sourceFiles | ForEach-Object { "$($_.path)|$($_.length)|$($_.sha256)" })
$targetComparable = @($targetFiles | ForEach-Object { "$($_.path)|$($_.length)|$($_.sha256)" })
$difference = @(Compare-Object $sourceComparable $targetComparable)
if ($difference.Count -ne 0) {
    throw "Seed copy differs from source: $($difference | ConvertTo-Json -Compress)"
}

$baselineDir = Join-Path $target 'docs\baseline'
$null = New-Item -ItemType Directory -Path $baselineDir -Force
[System.IO.File]::WriteAllLines((Join-Path $baselineDir 'source-status.txt'), $sourceStatus)

[ordered]@{
    schema_version = 1
    source_root = $source
    source_head = $sourceHead
    exclusions = @('.git', 'target', 'runtime', '*.log', '*.bak', '*.db')
    files = $sourceFiles
} | ConvertTo-Json -Depth 6 | Set-Content -LiteralPath (Join-Path $baselineDir 'source-3.6-manifest.json') -Encoding UTF8

[ordered]@{
    schema_version = 1
    target_root = $target
    source_head = $sourceHead
    exclusions = @('.git', 'target', 'runtime', '*.log', '*.bak', '*.db')
    files = $targetFiles
} | ConvertTo-Json -Depth 6 | Set-Content -LiteralPath (Join-Path $baselineDir 'target-3.8-manifest.json') -Encoding UTF8

Write-Output "PASS: $($sourceFiles.Count) seed files match"
```

Expected: `PASS: <count> seed files match` and both JSON files contain the same ordered `files` array.

- [ ] **Step 5: 固化目标仓库基线提交**

```powershell
$source = "D:\python-tools\重返未来1999\sonetto-rs"
$target = "D:\python-tools\重返未来1999\sonetto-rs-3.8-cn"

git -C $target add -A
if ($LASTEXITCODE -ne 0) { throw "Baseline staging failed" }

$stagedForbidden = @(git -C $target diff --cached --name-only | Where-Object {
    $_ -match '(^|/)(target|runtime)/' -or $_ -match '\.(log|bak|db)$'
})
if ($stagedForbidden.Count -ne 0) {
    throw "Forbidden paths staged: $($stagedForbidden -join ', ')"
}

git -C $target commit -m "chore: snapshot international 3.6 working baseline"
if ($LASTEXITCODE -ne 0) { throw "Baseline commit failed" }

$expectedSourceStatus = @(Get-Content -LiteralPath "$target\docs\baseline\source-status.txt")
$currentSourceStatus = @(git -C $source status --porcelain=v1 -uall)
$statusDifference = @(Compare-Object $expectedSourceStatus $currentSourceStatus)
if ($statusDifference.Count -ne 0) {
    throw "Source worktree changed during baseline copy"
}

git -C $target status --short
```

Expected: commit succeeds, source status is equivalent as a line set, and target status is empty.

- [ ] **Step 6: 在任何 3.8 改动前验证复制后的基线**

Run from `D:\python-tools\重返未来1999\sonetto-rs-3.8-cn`:

```powershell
cargo fmt --check
cargo check --workspace
cargo test --workspace
```

Expected: every command exits `0`. If any command fails, stop before Task 2 and report the baseline failure instead of changing 3.8 code.

### Task 2: 使用测试锁定 3.8 独立配置

**Files:**
- Modify: `common/src/config.rs`
- Modify: `common/Config.toml`

- [ ] **Step 1: 添加失败的嵌入式模板契约测试**

Append to `common/src/config.rs`:

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn bundled_template_is_cn_38_isolated() {
        let config: ServerConfig = toml::from_str(CONFIG_TEMPLATE).unwrap();

        assert_eq!(config.server.host, "127.0.0.1");
        assert_eq!(config.server.http_port, 21100);
        assert_eq!(config.server.game_port, 23401);
        assert_eq!(config.paths.data_dir, PathBuf::from("./data"));
        assert_eq!(
            config.paths.excel_data,
            PathBuf::from("./data/excel2json")
        );
        assert_eq!(config.paths.static_data, PathBuf::from("./data/static"));
        assert_eq!(
            config.database.path,
            PathBuf::from("./db/sonetto-3.8-cn.db")
        );
    }
}
```

- [ ] **Step 2: 运行测试并确认按预期失败**

Run from `D:\python-tools\重返未来1999\sonetto-rs-3.8-cn`:

```powershell
cargo test -p common config::tests::bundled_template_is_cn_38_isolated -- --exact
```

Expected: FAIL because the copied 3.6 template still uses HTTP `21000`, game TCP `23301`, or database `./db/sonetto.db`.

- [ ] **Step 3: 最小修改 3.8 配置模板**

In `common/Config.toml`, keep the existing banners and change only these values:

```toml
[server]
host = "127.0.0.1"
dns = "localhost"
http_port = 21100
game_port = 23401

[paths]
data_dir = "./data"
excel_data = "./data/excel2json"
static_data = "./data/static"

[database]
path = "./db/sonetto-3.8-cn.db"
```

- [ ] **Step 4: 运行配置测试并确认通过**

```powershell
cargo test -p common config::tests::bundled_template_is_cn_38_isolated -- --exact
```

Expected: one matching test passes with `test result: ok`.

- [ ] **Step 5: 提交独立配置**

```powershell
git add common/src/config.rs common/Config.toml
git commit -m "feat: isolate cn 3.8 server configuration"
```

Expected: commit includes only the config template and its contract test.

### Task 3: 测试驱动运行目录准备脚本

**Files:**
- Create: `scripts/tests/Test-PrepareCn38Runtime.ps1`
- Create: `scripts/Prepare-Cn38Runtime.ps1`
- Modify: `.gitignore`

- [ ] **Step 1: 编写失败的黑盒测试**

Create `scripts/tests/Test-PrepareCn38Runtime.ps1`:

```powershell
[CmdletBinding()]
param()

Set-StrictMode -Version Latest
$ErrorActionPreference = "Stop"

$prepareScript = Join-Path (Split-Path -Parent $PSScriptRoot) "Prepare-Cn38Runtime.ps1"
$tempRoot = Join-Path ([System.IO.Path]::GetTempPath()) ("sonetto-cn38-runtime-test-" + [guid]::NewGuid().ToString("N"))
$fakeRepo = Join-Path $tempRoot "repo"
$dataSource = Join-Path $tempRoot "data-source"
$runtimeRoot = Join-Path $fakeRepo "runtime"

function Assert-True {
    param(
        [Parameter(Mandatory = $true)][bool]$Condition,
        [Parameter(Mandatory = $true)][string]$Message
    )
    if (-not $Condition) { throw $Message }
}

try {
    if (-not (Test-Path -LiteralPath $prepareScript -PathType Leaf)) {
        throw "Prepare script not found: $prepareScript"
    }

    $null = New-Item -ItemType Directory -Path (Join-Path $fakeRepo "common") -Force
    $null = New-Item -ItemType Directory -Path (Join-Path $fakeRepo "target\debug") -Force
    $null = New-Item -ItemType Directory -Path (Join-Path $fakeRepo "assets\static") -Force
    $null = New-Item -ItemType Directory -Path (Join-Path $dataSource "excel2json") -Force

    [System.IO.File]::WriteAllText(
        (Join-Path $fakeRepo "common\Config.toml"),
        "[server]`nhost = `"127.0.0.1`"`ndns = `"localhost`"`nhttp_port = 21100`ngame_port = 23401`n[paths]`ndata_dir = `"./data`"`nexcel_data = `"./data/excel2json`"`nstatic_data = `"./data/static`"`n[database]`npath = `"./db/sonetto-3.8-cn.db`"`n"
    )
    [System.IO.File]::WriteAllText((Join-Path $fakeRepo "target\debug\sdkserver.exe"), "sdk")
    [System.IO.File]::WriteAllText((Join-Path $fakeRepo "target\debug\gameserver.exe"), "game")
    [System.IO.File]::WriteAllText((Join-Path $dataSource "excel2json\character.json"), "[]")
    [System.IO.File]::WriteAllText((Join-Path $fakeRepo "assets\static\marker.txt"), "static")
    [System.IO.File]::WriteAllText((Join-Path $dataSource "sonetto.db"), "must-not-copy")

    $first = & $prepareScript -RepositoryRoot $fakeRepo -DataSource $dataSource -RuntimeRoot $runtimeRoot | ConvertFrom-Json
    $second = & $prepareScript -RepositoryRoot $fakeRepo -DataSource $dataSource -RuntimeRoot $runtimeRoot | ConvertFrom-Json

    Assert-True ($first.http_port -eq 21100) "Unexpected HTTP port"
    Assert-True ($first.game_port -eq 23401) "Unexpected game port"
    Assert-True (Test-Path -LiteralPath (Join-Path $runtimeRoot "config.toml") -PathType Leaf) "Config was not copied"
    Assert-True (Test-Path -LiteralPath (Join-Path $runtimeRoot "sdkserver.exe") -PathType Leaf) "SDK binary was not copied"
    Assert-True (Test-Path -LiteralPath (Join-Path $runtimeRoot "gameserver.exe") -PathType Leaf) "Game binary was not copied"
    Assert-True (Test-Path -LiteralPath (Join-Path $runtimeRoot "data\excel2json\character.json") -PathType Leaf) "Excel data was not copied"
    Assert-True (Test-Path -LiteralPath (Join-Path $runtimeRoot "data\static\marker.txt") -PathType Leaf) "Static data was not copied"
    Assert-True (-not (Test-Path -LiteralPath (Join-Path $runtimeRoot "sonetto.db"))) "Source database was copied"
    Assert-True (@(Get-ChildItem -LiteralPath (Join-Path $runtimeRoot "db") -File).Count -eq 0) "Runtime database directory is not empty"
    Assert-True ($second.runtime_root -eq $first.runtime_root) "Repeated preparation changed the runtime root"

    Write-Output "PASS: CN 3.8 runtime preparation is isolated and repeatable"
}
finally {
    if (Test-Path -LiteralPath $tempRoot) {
        Remove-Item -LiteralPath $tempRoot -Recurse -Force
    }
}
```

- [ ] **Step 2: 运行测试并确认因脚本缺失而失败**

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\tests\Test-PrepareCn38Runtime.ps1
```

Expected: FAIL with `Prepare script not found`.

- [ ] **Step 3: 实现最小运行目录准备脚本**

Create `scripts/Prepare-Cn38Runtime.ps1`:

```powershell
[CmdletBinding()]
param(
    [Parameter(Mandatory = $true)]
    [string]$DataSource,

    [string]$RepositoryRoot = (Split-Path -Parent $PSScriptRoot),

    [string]$RuntimeRoot,

    [ValidateSet("debug", "release")]
    [string]$Profile = "debug"
)

Set-StrictMode -Version Latest
$ErrorActionPreference = "Stop"

function Resolve-RequiredDirectory {
    param([Parameter(Mandatory = $true)][string]$Path)
    if (-not (Test-Path -LiteralPath $Path -PathType Container)) {
        throw "Required directory not found: $Path"
    }
    (Resolve-Path -LiteralPath $Path).Path
}

function Resolve-RequiredFile {
    param([Parameter(Mandatory = $true)][string]$Path)
    if (-not (Test-Path -LiteralPath $Path -PathType Leaf)) {
        throw "Required file not found: $Path"
    }
    (Resolve-Path -LiteralPath $Path).Path
}

function Copy-DirectoryContent {
    param(
        [Parameter(Mandatory = $true)][string]$Source,
        [Parameter(Mandatory = $true)][string]$Destination
    )
    $null = New-Item -ItemType Directory -Path $Destination -Force
    $output = & robocopy.exe $Source $Destination /E /COPY:DAT /DCOPY:DAT /R:1 /W:1 /NFL /NDL /NJH /NJS /NP
    $exitCode = $LASTEXITCODE
    if ($exitCode -gt 7) {
        throw "Robocopy failed with exit code $exitCode`: $($output -join [Environment]::NewLine)"
    }
}

$repo = Resolve-RequiredDirectory -Path $RepositoryRoot
$data = Resolve-RequiredDirectory -Path $DataSource
if ([string]::IsNullOrWhiteSpace($RuntimeRoot)) {
    $RuntimeRoot = Join-Path $repo "runtime"
}
$runtime = [System.IO.Path]::GetFullPath($RuntimeRoot)

$config = Resolve-RequiredFile -Path (Join-Path $repo "common\Config.toml")
$binaryDirectory = Resolve-RequiredDirectory -Path (Join-Path $repo "target\$Profile")
$sdkBinary = Resolve-RequiredFile -Path (Join-Path $binaryDirectory "sdkserver.exe")
$gameBinary = Resolve-RequiredFile -Path (Join-Path $binaryDirectory "gameserver.exe")
$excelSource = Resolve-RequiredDirectory -Path (Join-Path $data "excel2json")
$staticSource = Resolve-RequiredDirectory -Path (Join-Path $repo "assets\static")

$configText = [System.IO.File]::ReadAllText($config)
if ($configText -notmatch '(?m)^http_port\s*=\s*21100\s*$') {
    throw "Config does not declare HTTP port 21100"
}
if ($configText -notmatch '(?m)^game_port\s*=\s*23401\s*$') {
    throw "Config does not declare game port 23401"
}
if ($configText -notmatch '(?m)^path\s*=\s*"\./db/sonetto-3\.8-cn\.db"\s*$') {
    throw "Config does not declare the isolated CN 3.8 database"
}

$null = New-Item -ItemType Directory -Path $runtime -Force
$null = New-Item -ItemType Directory -Path (Join-Path $runtime "db") -Force
Copy-Item -LiteralPath $config -Destination (Join-Path $runtime "config.toml") -Force
Copy-Item -LiteralPath $sdkBinary -Destination (Join-Path $runtime "sdkserver.exe") -Force
Copy-Item -LiteralPath $gameBinary -Destination (Join-Path $runtime "gameserver.exe") -Force
Copy-DirectoryContent -Source $excelSource -Destination (Join-Path $runtime "data\excel2json")
Copy-DirectoryContent -Source $staticSource -Destination (Join-Path $runtime "data\static")

[ordered]@{
    runtime_root = $runtime
    profile = $Profile
    http_port = 21100
    game_port = 23401
    database_path = (Join-Path $runtime "db\sonetto-3.8-cn.db")
    database_exists = (Test-Path -LiteralPath (Join-Path $runtime "db\sonetto-3.8-cn.db"))
} | ConvertTo-Json -Compress
```

- [ ] **Step 4: 忽略独立运行产物**

Append to `.gitignore`:

```gitignore

# CN 3.8 local runtime
/runtime/
*.log
*.err.log
*.bak
*.db
```

- [ ] **Step 5: 运行黑盒测试并确认通过**

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\tests\Test-PrepareCn38Runtime.ps1
```

Expected: `PASS: CN 3.8 runtime preparation is isolated and repeatable`.

- [ ] **Step 6: 提交运行目录工具**

```powershell
git add .gitignore scripts/Prepare-Cn38Runtime.ps1 scripts/tests/Test-PrepareCn38Runtime.ps1
git commit -m "feat: prepare isolated cn 3.8 runtime"
```

Expected: commit contains the preparation script, its black-box test, and runtime ignore rules.

### Task 4: 构建基线并生成实际运行目录

**Files:**
- Create, ignored: `runtime/config.toml`
- Create, ignored: `runtime/sdkserver.exe`
- Create, ignored: `runtime/gameserver.exe`
- Create, ignored: `runtime/data/excel2json/**`
- Create, ignored: `runtime/data/static/**`
- Create, ignored: `runtime/db/`

- [ ] **Step 1: 运行格式检查**

```powershell
cargo fmt --check
```

Expected: exit code `0` and no formatting diff.

- [ ] **Step 2: 检查整个工作区**

```powershell
cargo check --workspace
```

Expected: exit code `0`; checked-in generated configs may emit a warning when `data/excel2json` is absent, but compilation succeeds.

- [ ] **Step 3: 运行整个工作区测试**

```powershell
cargo test --workspace
```

Expected: all test binaries finish with `test result: ok`; no test writes to the international 3.6 database.

- [ ] **Step 4: 构建两个服务端二进制**

```powershell
cargo build -p sdkserver -p gameserver
```

Expected: `target/debug/sdkserver.exe` and `target/debug/gameserver.exe` exist.

- [ ] **Step 5: 从独立数据副本准备运行目录**

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\Prepare-Cn38Runtime.ps1 `
    -DataSource "D:\python-tools\重返未来1999\sonetto-data" `
    -RepositoryRoot "D:\python-tools\重返未来1999\sonetto-rs-3.8-cn" `
    -RuntimeRoot "D:\python-tools\重返未来1999\sonetto-rs-3.8-cn\runtime" `
    -Profile debug
```

Expected JSON contains `http_port: 21100`, `game_port: 23401`, and `database_exists: false`.

- [ ] **Step 6: 验证实际运行目录没有继承数据库**

```powershell
$runtime = "D:\python-tools\重返未来1999\sonetto-rs-3.8-cn\runtime"
$required = @(
    "$runtime\config.toml",
    "$runtime\sdkserver.exe",
    "$runtime\gameserver.exe",
    "$runtime\data\excel2json",
    "$runtime\data\static",
    "$runtime\db"
)
foreach ($path in $required) {
    if (-not (Test-Path -LiteralPath $path)) { throw "Missing runtime path: $path" }
}
$databaseFiles = @(Get-ChildItem -LiteralPath "$runtime\db" -File)
if ($databaseFiles.Count -ne 0) {
    throw "Runtime database directory must start empty: $($databaseFiles.FullName -join ', ')"
}
$trackedRuntime = @(git ls-files runtime)
if ($trackedRuntime.Count -ne 0) {
    throw "Runtime files are tracked: $($trackedRuntime -join ', ')"
}
Write-Output "PASS: runtime layout is complete and database-free"
```

Expected: `PASS: runtime layout is complete and database-free`.

### Task 5: 记录 3.8 基线工作流并完成回归检查

**Files:**
- Create: `docs/CN_3.8_DEVELOPMENT.md`

- [ ] **Step 1: 写入基线操作文档**

Create `docs/CN_3.8_DEVELOPMENT.md`:

````markdown
# 国服 3.8 开发基线

## 固定客户端

- APK：`D:\python-tools\重返未来1999\重返未来：1999.apk`
- 包名：`com.shenlan.m.reverse1999`
- 版本：`3.8.0` / `190`
- SHA-256：`EA6CD8AD7FAAFE6EDA42F4C2073DCF1BDA7F24AAE9A7011FDFE918AFFF69D3C0`

## 本地服务

- SDK HTTP：`127.0.0.1:21100`
- 游戏 TCP：`127.0.0.1:23401`
- 数据库：`runtime/db/sonetto-3.8-cn.db`

所有服务只监听 loopback。不得修改 Windows、WinHTTP、环境变量或 Android 代理设置。

## 验证基线

```powershell
cargo fmt --check
cargo check --workspace
cargo test --workspace
cargo build -p sdkserver -p gameserver
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\tests\Test-PrepareCn38Runtime.ps1
```

## 准备运行目录

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\Prepare-Cn38Runtime.ps1 `
    -DataSource "D:\python-tools\重返未来1999\sonetto-data" `
    -RepositoryRoot $PWD.Path `
    -RuntimeRoot (Join-Path $PWD.Path "runtime") `
    -Profile debug
```

初始 `runtime/data` 是国际服 3.6 数据的独立副本，只用于 3.8 服务启动和协议分析。它不能作为大厅适配完成时的最终 3.8 数据。

运行目录首次创建时，`runtime/db` 必须为空。启动服务后产生的数据库只属于 3.8，不得复制回国际服 3.6 目录。
````

- [ ] **Step 2: 提交开发文档**

```powershell
git add docs/CN_3.8_DEVELOPMENT.md
git commit -m "docs: document cn 3.8 development baseline"
```

Expected: commit contains only the new development document.

- [ ] **Step 3: 复跑阶段 0 验证**

```powershell
cargo fmt --check
cargo check --workspace
cargo test --workspace
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\tests\Test-PrepareCn38Runtime.ps1
```

Expected: every command exits `0`; PowerShell test prints its `PASS` line.

- [ ] **Step 4: 证明国际服 3.6 源工作树没有变化**

```powershell
$source = "D:\python-tools\重返未来1999\sonetto-rs"
$target = "D:\python-tools\重返未来1999\sonetto-rs-3.8-cn"
$expectedStatus = @(Get-Content -LiteralPath "$target\docs\baseline\source-status.txt")
$actualStatus = @(git -C $source status --porcelain=v1 -uall)
$difference = @(Compare-Object $expectedStatus $actualStatus)
if ($difference.Count -ne 0) {
    throw "International 3.6 source changed: $($difference | ConvertTo-Json -Compress)"
}
Write-Output "PASS: international 3.6 source status is unchanged"
```

Expected: `PASS: international 3.6 source status is unchanged`.

- [ ] **Step 5: 检查目标提交和工作区状态**

```powershell
git log -5 --oneline --decorate
git status --short
git check-ignore -v runtime\config.toml runtime\db\sonetto-3.8-cn.db
```

Expected: recent history includes the baseline snapshot, isolated config, runtime preparation, and documentation commits; `git status --short` is empty; both runtime examples are ignored.

## 阶段 0 完成条件

- `sonetto-rs-3.8-cn` 是独立 Git 仓库，分支为 `feat/cn-3.8-login-lobby`。
- 当前可运行的国际服 3.6 工作树已在目标仓库固化为可审计基线提交。
- 国际服 3.6 源目录的 Git 状态与复制前一致。
- 3.8 使用 HTTP `21100`、游戏 TCP `23401` 和 `sonetto-3.8-cn.db`。
- 运行目录准备脚本经过黑盒测试，并且不复制任何旧数据库。
- `cargo fmt --check`、`cargo check --workspace` 和 `cargo test --workspace` 全部通过。
- 实际 `runtime` 布局完整、数据库目录初始为空且全部运行产物被 Git 忽略。
